import { Preample } from "../../components/Preample";

export default Preample;

<TRef external={{
  newton: ["ニュートンの二項定理", "https://mathtrain.jp/genenikou"],
}} />

## 二項定理と拡張

このページでは次の_定理_を**証明**する。

<Theorem name="二項定理">

  <TRef label="binomial" />

  $n \in \mathbb{N}$に対して

  $$
    (a + b)^n = \sum_{k = 0}^n {}_n C_r a^k b^{n - k}
  $$
</Theorem>

<Theorem name="多項定理">

  <TRef label="multinomial" />

  $k, n \in \mathbb{N}$に対して

  $$
    (a_1 + a_2 + \cdots + a_k)^n = \sum_{i_1 + i_2 + \cdots + i_n = n} \frac{n!}{i_1! i_2! \cdots i_n!} a_1^{i_1} a_2^{i_2} \cdots a_n^{i_n}
  $$
</Theorem>

ただし、${}_n C_r$は次のように定義する。

<Definition name="二項係数">

  $n$個のものから$r$個だけ選ぶ選び方の総数を${}_n C_r$と表す。
</Definition>

まずは<TRef use="binomial" name />を考えよう。<TRef use="multinomial" />はその拡張である。

別の方向の拡張としては<TRef use="newton" name />がある。

<Proof>

  aaaaaaa

  省略する。
</Proof>

<Question>

  証明してみよう！

  <Answer expansion={true}>

  一般には面倒    

  </Answer>

</Question>
